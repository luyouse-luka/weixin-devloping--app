<view class="choose-chef-page">
  <!-- 顶部装饰 -->
  <view class="top-decoration"></view>
  
  <!-- 返回按钮 -->
  <view class="back-container">
    <view class="back-btn" bindtap="goBack">
      <image class="back-icon-img" src="data:image/svg+xml;base64," mode="widthFix"></image>
      <text class="back-text">返回</text>
    </view>
  </view>

  <!-- 页面标题 -->
  <view class="page-header">
    <view class="title-icon">👨‍🍳</view>
    <text class="page-title">今天的饭交给谁呢</text>
    <text class="page-subtitle">哪位选手可以出战</text>
  </view>

  <!-- 菜品信息提示 -->
  <view class="order-info-card">
    <view class="info-row">
      <text class="info-label">已选菜品</text>
      <text class="info-value">{{favorites.length}} 道</text>
    </view>
  </view>

  <!-- 家人列表 -->
  <view class="chefs-section">
    <view class="section-title">选手列表</view>
    <view class="chefs-list">
      <block wx:for="{{chefs}}" wx:key="id">
        <view 
          class="chef-card {{selectedChefId === item.id ? 'selected' : ''}}"
          bindtap="selectChef"
          data-id="{{item.id}}"
        >
          <!-- 选中标记 -->
          <view class="selected-badge" wx:if="{{selectedChefId === item.id}}">
            <text class="check-icon">✓</text>
          </view>

          <!-- 厨师头像 -->
          <view class="chef-avatar-wrapper">
            <image src="{{item.avatar}}" mode="aspectFill" class="chef-avatar"></image>
            <view class="rating-badge">
              <view class="rating-text"> {{item.rating}}</view>
            </view>
          </view>

          <!-- 厨师信息 -->
          <view class="chef-info">
            <view class="chef-header">
              <text class="chef-name">{{item.name}}</text>
              <view class="chef-badges">
                <view 
                  class="specialty-badge"
                  wx:for="{{item.specialties}}"
                  wx:key="*this"
                  wx:for-item="specialty"
                  wx:if="{{specialty}}"
                >
                  {{specialty}}
                </view>
              </view>
            </view>
            
            <text class="chef-description">{{item.description}}</text>
            
            <view class="chef-stats">
              <text class="stat-item">已经做过 {{item.ordersCount >= 999 ? '999+' : item.ordersCount}} 次</text>
            </view>
          </view>

          <!-- 选择按钮 -->
          <view class="select-btn-wrapper">
            <view class="select-btn {{selectedChefId === item.id ? 'selected' : ''}}">
              <text wx:if="{{selectedChefId === item.id}}">已选择</text>
              <text wx:else>选择</text>
            </view>
          </view>
        </view>
      </block>
    </view>
  </view>

  <!-- 底部确认按钮 -->
  <view class="footer-container">
    <view class="selected-info" wx:if="{{selectedChef}}">
      <text class="selected-text">已选择：{{selectedChef.name}}</text>
    </view>
    <button class="confirm-btn" bindtap="confirmAndSubmit">
      <text class="btn-icon">✓</text>
      <text class="btn-text">确认安排</text>
    </button>
  </view>
</view>


